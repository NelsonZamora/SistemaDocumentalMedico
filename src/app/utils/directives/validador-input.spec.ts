import { ValidadorInput } from './validador-input';

describe('ValidadorInput', () => {
  it('should create an instance', () => {
    const directive = new ValidadorInput();
    expect(directive).toBeTruthy();
  });
});
